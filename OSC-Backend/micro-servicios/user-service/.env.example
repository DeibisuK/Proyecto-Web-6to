# Server configuration
PORT=3001

# Database configuration
DB_HOST=your_db_host
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name
DB_PORT=25060

# Encryption key for payment methods (MUST be exactly 32 characters)
# ⚠️ IMPORTANT: Generate a secure unique key for production
# You can use: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
ENCRYPTION_KEY=clave-muy-segura-de-32-chars!!
GOOGLE_APPLICATION_CREDENTIALS=C:\Users\Otako\Documents\proyect-osc-firebase-adminsdk.json
